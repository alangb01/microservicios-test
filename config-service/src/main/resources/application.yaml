server:
  post: 8080

spring:
  cloud:
    config:
      server:
        git:
          default-label: config-server
          uri:  https://github.com/alangb01/microservicios-test
          search-paths: config-data
  application:
    name: config-service
